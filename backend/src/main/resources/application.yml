server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: energy-management
  datasource:
    url: jdbc:postgresql://localhost:5432/energy_management
    username: postgres
    password: ltqltq2005
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 30000
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    serialization:
      write-dates-as-timestamps: false
  servlet:
    multipart:
      max-file-size: 2048MB
      max-request-size: 2048MB

# AI Model Configuration (supports OpenAI-compatible APIs)
ai:
  api:
<<<<<<< HEAD
    base-url: ${AI_API_BASE_URL:http://localhost:8000}
    api-key: ${AI_API_API_KEY:}
    model: ${AI_API_MODEL:qwen3.5:2b}
    timeout: ${AI_API_TIMEOUT:120}
    max-tokens: ${AI_API_MAX_TOKENS:4096}
=======
    base-url: ${AI_API_BASE_URL}
    api-key: ${AI_API_API_KEY}
    model: ${AI_API_MODEL}
    timeout: ${AI_API_TIMEOUT}
    max-tokens: ${AI_API_MAX_TOKENS}
>>>>>>> develop

# CSV data import path
data:
  import:
    base-path: ./data/csv

# Logging
logging:
  level:
    com.energy.management: DEBUG
    org.hibernate.SQL: WARN
